[pytest]
asyncio_mode = auto
addopts = -v --tb=short -ra
testpaths = tests
markers =
    slow: marks tests as slow
    integration: integration tests
    unit: unit tests
    crud: crud tests
    api: api tests
    services: service tests
    routes: route tests
